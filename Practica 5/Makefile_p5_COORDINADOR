#*****************************************************************
# File:   Makefile_p5
# Author: PSCD-Unizar
# Date:   noviembre 2020
# Coms:   Ejemplo de Makefile para la práctica 5 de PSCD
#         "make -f Makefile_p5" construye todos los ejecutables
#         "make -f Makefile_p5 Estudiante" construye "Estudiante"
#         ....
#         Es necesario descomentar la línea 28 para poder compilar en hendrix
#*****************************************************************

#---------------------------------------------------------
CC=g++


COOR=practica_5_COORDINADOR
CP=ControlParejas
SOCKET_DIR=Socket
SOCKET=${SOCKET_DIR}/Socket

CPPFLAGS=-I. -I${SOCKET_DIR} -std=c++11 -lsockets # Flags compilacion
LDFLAGS=-pthread # Flags linkado threads

all: ${EST} ${COOR} 
#----------------------------------------------------------------------------
#Para gestionar opciones de compilación según la máquina: hendrix tiene sus manías
#Descomentar la siguiente línea para compilar en hendrix
SOCKETSFLAGS=-lsocket -lnsl
#-----------------------------------------------------------
# Coordinador
# Compilacion
${COOR}.o: ${COOR}.cpp 
	${CC} -c ${CPPFLAGS} ${COOR}.cpp

# Linkado
${COOR}: ${SOCKET}.o ${COOR}.o ${CP}.o
	${CC} ${LDFLAGS} ${SOCKET}.o ${COOR}.o ${CP}.o -o ${COOR} ${SOCKETSFLAGS}
#-----------------------------------------------------------
# ControlParejas
# Compilacion
${CP}.o: ${CP}.hpp ${CP}.cpp
	${CC} -c ${CP}.cpp -o ${CP}.o ${CPPFLAGS}


# SOCKETS
# Compilacion libreria de Sockets
${SOCKET}.o: ${SOCKET}.hpp ${SOCKET}.cpp
	${CC} -c ${CPPFLAGS} ${SOCKET}.cpp -o ${SOCKET}.o
#-----------------------------------------------------------	
# LIMPIEZA
clean:
	$(RM) ${SOCKET}.o
	$(RM) ${COOR} ${COOR}.o
	$(RM) ${CP} ${CP}.o
